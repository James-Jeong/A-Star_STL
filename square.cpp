#include "square.h"

Square::Square(int zz, int zo, int zt, int oz, int ot, int tz, int to, int tt)
{
    this->zero_zero = zz; this->zero_one = zo; this->zero_two = zt;
    this->one_zero = oz; this->one_two = ot;
    this->two_zero = tz; this->two_one = to; this->two_two = tt;
}
